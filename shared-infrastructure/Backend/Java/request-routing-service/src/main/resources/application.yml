server:
  port: 8327

gogidix:
  security:
    supabase:
      issuer-uri: ${GOGIDIX_SUPABASE_ISSUER_URI:}
      audience: ${GOGIDIX_SUPABASE_AUDIENCE:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
