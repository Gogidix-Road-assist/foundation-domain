server:
  port: 8316

gogidix:
  audit:
    retention: ${GOGIDIX_AUDIT_RETENTION:90d}
    mongo-enabled: ${GOGIDIX_AUDIT_MONGO_ENABLED:false}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
