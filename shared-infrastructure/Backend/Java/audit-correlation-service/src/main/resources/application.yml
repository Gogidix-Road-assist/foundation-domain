server:
  port: 8314

gogidix:
  correlation:
    retention: ${GOGIDIX_CORRELATION_RETENTION:90d}
    mongo-enabled: ${GOGIDIX_CORRELATION_MONGO_ENABLED:false}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
