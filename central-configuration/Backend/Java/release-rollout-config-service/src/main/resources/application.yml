server:
  port: 8105

spring:
  application:
    name: release-rollout-config-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://gazalgidix_db_user:Ajimmy2907@cluster0.ggksiwg.mongodb.net/release_rollout_db?appName=Cluster0}
      auto-index-creation: true
  redis:
    host: ${REDIS_HOST:r-4xo326106227c264.redis.germany.rds.aliyuncs.com}
    port: 6379
    password: ${REDIS_PASSWORD:Gog1d1xPlatform2026}
    database: 7

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    com.gogidix.rapidassist.release.rollout.config.service: INFO

rollout:
  default:
    batch-size: 10
    batch-interval-minutes: 30
