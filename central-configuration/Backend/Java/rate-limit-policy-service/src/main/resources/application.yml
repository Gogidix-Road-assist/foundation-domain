server:
  port: 8104

spring:
  application:
    name: rate-limit-policy-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://gazalgidix_db_user:Ajimmy2907@cluster0.ggksiwg.mongodb.net/rate_limit_policy_db?appName=Cluster0}
      auto-index-creation: true
    redis:
      host: ${REDIS_HOST:r-4xo326106227c264.redis.germany.rds.aliyuncs.com}
      port: 6379
      password: ${REDIS_PASSWORD:Gog1d1xPlatform2026}
      database: 6

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    com.gogidix.rapidassist.rate.limit.policy.service: INFO

rate-limit:
  default:
    requests-per-minute: 60
  cache:
    ttl-seconds: 60
