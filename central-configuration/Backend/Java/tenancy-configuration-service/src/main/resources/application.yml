server:
  port: 8106

spring:
  application:
    name: tenancy-configuration-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://gazalgidix_db_user:Ajimmy2907@cluster0.ggksiwg.mongodb.net/tenancy_configuration_db?appName=Cluster0}
      auto-index-creation: true
  redis:
    host: ${REDIS_HOST:r-4xo326106227c264.redis.germany.rds.aliyuncs.com}
    port: 6379
    password: ${REDIS_PASSWORD:Gog1d1xPlatform2026}
    database: 8

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    com.gogidix.rapidassist.tenancy.configuration.service: INFO

tenancy:
  cache:
    ttl-minutes: 30
  default:
    max-users-per-tenant: 1000
