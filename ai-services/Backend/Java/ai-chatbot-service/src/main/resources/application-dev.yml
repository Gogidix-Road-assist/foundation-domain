spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rapidassist_ai_chatbot_dev
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    com.gogidix.rapidassist.ai.chatbot: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
