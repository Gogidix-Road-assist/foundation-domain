package com.gogidix.rapidassist.ai.matching.domain.model;

/**
 * Enum representing different matching algorithm types.
 */
public enum MatchingAlgorithmType {
    FUZZY_MATCHING,
    SEMANTIC_MATCHING,
    EXACT_MATCHING,
    PHONETIC_MATCHING,
    TF_IDF,
    COSINE_SIMILARITY,
    JACCARD_SIMILARITY,
    LEVENSHTEIN_DISTANCE,
    EUCLIDEAN_DISTANCE,
    RECORD_LINKAGE,
    DEDUPLICATION,
    CUSTOM_RULE
}
